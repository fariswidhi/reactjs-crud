import React, { Component, PropTypes } from 'react';

class Register extends Component {
 
    constructor(props) {
        super(props);
    }

    render() {
        return (
            
        );
    }
}

export default Register;
